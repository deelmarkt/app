{
  "name": "deelmarkt",
  "version": "0.3.0",
  "private": true,
  "description": "DeelMarkt — powered by Antigravity AI Kit",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "sync-version": "node scripts/sync-version.mjs",
    "sync-version:dry": "node scripts/sync-version.mjs --dry-run",
    "sync-version:check": "node scripts/sync-version.mjs --check"
  }
}
